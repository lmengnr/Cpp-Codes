This code will eventually be ammended to create a C++ grep program  

At the moment the code reads a file line by line into a vector  

##Update  
The code has been amended to perform the same as grep

